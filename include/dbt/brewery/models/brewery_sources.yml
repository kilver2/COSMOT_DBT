version: 2

sources:
  - name: bronze
    catalog: brewery
    schema: bronze
    tables:
      - name: raw_breweries
        description: Raw brewery API ingestion

        loaded_at_field: _ingested_at

        freshness:
          warn_after:
            count: 1
            period: day

          error_after:
            count: 7
            period: day